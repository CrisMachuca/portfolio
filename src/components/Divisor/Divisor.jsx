import React from 'react';

import styles from "./Divisor.module.css";
import { getImageUrl } from '../../utils';

export const Divisor = () => {
  return (
    <section className={styles.container} id="divisor">
        <div className={styles.rotate1}></div>  
    </section>
  );
};

